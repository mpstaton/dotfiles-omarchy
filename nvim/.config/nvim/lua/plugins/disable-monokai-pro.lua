-- gthelding/monokai-pro.nvim was deleted from GitHub, so lazy.nvim fails to clone
-- it on every launch. all-themes.lua keeps it only for Omarchy 3.8 themes, which
-- this Omarchy 4 install doesn't use. Remove this file if the repo comes back.
return {
	"gthelding/monokai-pro.nvim",
	enabled = false,
}
